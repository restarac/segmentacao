<h1>New Segmentation</h1>

<%= form_for(@segmentation) do |f| %>

  <%= render 'form_messages', segmentation: @segmentation %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= submit_tag 'Add conditions' %>
  </div>
<% end %>

<%= link_to 'Back', segmentations_path %>
